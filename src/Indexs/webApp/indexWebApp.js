import React, { Component } from 'react';
import Login from '../../webAppComponents/login/Login';

export default class indexWebApp extends Component {
  render(){

    return(
		<Login/>
    );
  }
}